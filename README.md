This is a more complete implementation of a Deque (double-ended queue) data structure for Nim.
It is intended to replace `std/deques`.
HTML documentation can be created by running `nim doc deque.nim`
